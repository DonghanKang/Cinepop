<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mp.cinepop.account.model.AccountDAO">
	<insert id="insertAccount" parameterType="accountVo">
		<selectKey keyProperty="a_no" resultType="int" order="BEFORE">
			select account_seq.nextval from dual
		</selectKey>
		insert into account(a_no,a_name,id,postcode1,address_1,detailAddress1,tel)
		values(#{a_no},#{a_name},#{id},#{postcode1},#{address_1},#{detailAddress1},#{tel})
	</insert>
</mapper>